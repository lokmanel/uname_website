<section class="page-content" style="padding: var(--sp-xl) var(--gutter);">
  <h1>{{ page.title }}</h1>
  <div>{{ page.content }}</div>
</section>

{% schema %}
{
  "name": "Contenu de page",
  "tag": "section"
}
{% endschema %}
